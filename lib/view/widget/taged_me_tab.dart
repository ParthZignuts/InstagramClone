import 'package:flutter/material.dart';
class TagedMeTab extends StatelessWidget {
  const TagedMeTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Taged Me "),);
  }
}
